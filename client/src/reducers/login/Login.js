// import { CAPTURE_LOGIN_FIELD_VALUES } from '../../actions';

/**
 * Reducers
 *
 * NOTE: state should have a property for each component
 *       ie; state.login.componentState
 *       
 * NOTE: You'll often find that you need to store some data, 
 *       as well as some UI state, in the state tree. This is fine, 
 *       but try to keep the data separate from the UI state.
 * 
 */

// TODO: comment/doc this
// login validation reducer
// (previousState, action) => newState
// const validate = (state, action) => {
//   switch (action.type) {
//     // alt naming conventions: update_field | capture_Field
//     case CAPTURE_LOGIN_FIELD_VALUES:
//       return {
//         ...state,
//         fields: {
//           ...state.fields,
//           ...action.newFields,
//         }
//       };
//     default:
//       return state;
//   }
// };

// export default validate;
